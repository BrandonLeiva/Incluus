// Archivo: script.js
function startLesson(lessonName) {
    alert(`Iniciando ${lessonName}`);
    // Aquí podrías redirigir a la página de la lección o cargar contenido adicional.
    // window.location.href = `/leccion/${lessonName.toLowerCase().replace(" ", "_")}`;
}
